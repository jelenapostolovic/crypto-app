<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Crypto</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="#">Crypto Dashboard</a>
        <div class="collapse navbar-collapse">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a class="nav-link" href="/api/crypto-currency/{symbol}">Search API Cryptos</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/api/crypto-currency">MIN/MAX API Cryptos</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/api/cryptos">API Cryptos</a>
                </li>
            </ul>
        </div>
    </nav>

    <div class="container mt-5">
        <h1 class="text-center">Update Crypto</h1>

        <form action="{{ path('crypto_update', {'id': crypto.id}) }}" method="post">
            <div class="form-group">
                <label for="name">Name</label>
                <input type="text" id="name" name="name" class="form-control" value="{{ crypto.name }}" required>
            </div>
            <div class="form-group">
                <label for="symbol">Symbol</label>
                <input type="text" id="symbol" name="symbol" class="form-control" value="{{ crypto.symbol }}" required>
            </div>
            <div class="form-group">
                <label for="price">Current Price</label>
                <input type="number" id="price" name="price" class="form-control" step="0.01" value="{{ crypto.currentPrice }}" required>
            </div>
            <div class="form-group">
                <label for="totalVolume">Total Volume</label>
                <input type="number" id="totalVolume" name="totalVolume" class="form-control" step="0.01" value="{{ crypto.totalVolume }}" required>
            </div>
            <div class="form-group">
                <label for="ath">ATH</label>
                <input type="number" id="ath" name="ath" class="form-control" step="0.01" value="{{ crypto.ath }}" required>
            </div>
            <div class="form-group">
                <label for="athDate">ATH Date</label>
                <input type="date" id="athDate" name="athDate" class="form-control" value="{{ crypto.athDate ? crypto.athDate|date('Y-m-d') : '' }}">
            </div>
            <div class="form-group">
                <label for="atl">ATL</label>
                <input type="number" id="atl" name="atl" class="form-control" step="0.01" value="{{ crypto.atl }}" required>
            </div>
            <div class="form-group">
                <label for="atlDate">ATL Date</label>
                <input type="date" id="atlDate" name="atlDate" class="form-control" value="{{ crypto.atlDate ? crypto.atlDate|date('Y-m-d') : '' }}">
            </div>

            <button type="submit" class="btn btn-primary">Update Crypto</button>
        </form>
    </div>

    <footer class="bg-light text-center text-lg-start mt-5">
        <div class="text-center p-3">
            © 2024 Crypto Dashboard | <a href="/api/cryptos">API</a>
        </div>
    </footer>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
